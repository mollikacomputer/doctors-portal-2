import React from 'react';
import AppointmentBanner from './AppointmentBanner';

const Appointments = () => {
    return (
        <div>
            <AppointmentBanner/>
        </div>
    );
};

export default Appointments;